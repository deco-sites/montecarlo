import type { Props } from "../components/search/Content.tsx";
import Component from "../components/search/Content.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
