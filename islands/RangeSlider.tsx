import type { Props } from "../components/ui/RangeSlider.tsx";
import Component from "../components/ui/RangeSlider.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
